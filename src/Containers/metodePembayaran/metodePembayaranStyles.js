import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  dropDownStyle: {
    marginTop: -10,
  },
  buttonStyle: {
    height: 50,
    borderRadius: 100,
    margin: 10,
  }
});